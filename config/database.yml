development:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  pool: 5
  username: root
  host: localhost
  database: burn_me_down_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  pool: 5
  username: root
  host: localhost
  database: burn_me_down_test
