%header
  %h1
    = link_to "BurnMeDown", root_path
  %nav
    %ul
      %li
        = link_to "Teams", teams_path
      %li
        = link_to current_user.email, edit_user_registration_path
      %li
        - if user_signed_in?
          = link_to destroy_user_session_path, method: :delete do
            %i.fa.fa-sign-out
            Sign out

